pipeline {
    agent any

    stages {

        stage('Welcome') {
            steps {
                echo 'Hello DevOps'
            }
        }

        stage('List Files') {
            steps {
                sh 'pwd'
                sh 'ls -la'
            }
        }

    }
}
